interface:
  display_name: "OpenCLI Web Router"
  short_description: "Automatically route supported website tasks via OpenCLI"
  default_prompt: "Use $opencli-web to find the matching site and terminal contract, then run only an exact OpenCLI command declared there."

policy:
  allow_implicit_invocation: true
