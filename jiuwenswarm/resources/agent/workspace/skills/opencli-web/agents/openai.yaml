interface:
  display_name: "OpenCLI Web Router"
  short_description: "Automatically route supported website tasks via OpenCLI"
  default_prompt: "Use $opencli-web automatically to route this live website task through the matching bundled site router and operation contract."

policy:
  allow_implicit_invocation: true
