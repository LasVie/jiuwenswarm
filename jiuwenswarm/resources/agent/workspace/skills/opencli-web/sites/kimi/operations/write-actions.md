# Kimi: write-actions

Change remote service state.

| Command | Effect / risk | Exact usage | Arguments | Runtime |
|---|---|---|---|---|
| `dismiss-banner` | `reversible_remote_write` / `high` | `opencli kimi dismiss-banner -f json`<br>Close any visible sidebar banner (e.g., "Make a Review & Earn Credit", "获取应用程序") by clicking its Close svg. | none | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `history-rename` | `reversible_remote_write` / `high` | `opencli kimi history-rename "<chat-id>" "<new-title>" [--yes <true\|false>] -f json`<br>Rename a chat from the /chat/history page (clicks the inline Edit svg next to a chat row, types the new title, and saves). Requires --yes. | `chat-id` (str, required, positional); `new-title` (str, required, positional); `yes` (boolean, optional, default=False) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `mode` | `reversible_remote_write` / `high` | `opencli kimi mode ["<name>"] -f json`<br>Switch to a Kimi work mode: ppt \| docs \| deep-research \| websites \| sheets \| agent-swarm \| code. With no argument, lists modes. | `name` (str, optional, positional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `model` | `reversible_remote_write` / `high` | `opencli kimi model [--list <true\|false>] [--set "<set>"] -f json`<br>Read the current Kimi model (e.g. "K2.6 思考") or switch by clicking the model dropdown. With no argument, returns current; with --list, opens dropdown + lists; with --set <name>, switches. | `list` (boolean, optional, default=False); `set` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `react` | `reversible_remote_write` / `high` | `opencli kimi react "<kind>" [--conv "<conv>"] -f json`<br>Like or dislike the last assistant message. Pass --conv <id> to target a specific chat. | `kind` (str, required, positional); `conv` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `regenerate` | `reversible_remote_write` / `high` | `opencli kimi regenerate [--conv "<conv>"] -f json`<br>Click Refresh (Kimi's regenerate button) on the last assistant message. Pass --conv <id> to target a specific chat. | `conv` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `settings` | `reversible_remote_write` / `high` | `opencli kimi settings -f json`<br>Open the Kimi settings page (/settings). | none | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `share` | `reversible_remote_write` / `high` | `opencli kimi share [--conv "<conv>"] -f json`<br>Click Share on the last assistant message (opens Kimi's share dialog). Pass --conv <id> to target a specific chat. | `conv` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `sidebar-toggle` | `reversible_remote_write` / `high` | `opencli kimi sidebar-toggle -f json`<br>Click the LeftBar svg to toggle the Kimi sidebar. | none | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `sign-out` | `reversible_remote_write` / `high` | `opencli kimi sign-out [--yes <true\|false>] -f json`<br>Click SignOut on the Kimi /settings page. Navigates to /settings first if not already there. Requires --yes. | `yes` (boolean, optional, default=False) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `upgrade` | `reversible_remote_write` / `high` | `opencli kimi upgrade -f json`<br>Click the "Upgrade" button (or 升级会员) in the Kimi sidebar — opens the membership/upgrade page or dialog. | none | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `view-all-history` | `reversible_remote_write` / `high` | `opencli kimi view-all-history -f json`<br>Navigate to /chat/history (full conversation list page). | none | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
