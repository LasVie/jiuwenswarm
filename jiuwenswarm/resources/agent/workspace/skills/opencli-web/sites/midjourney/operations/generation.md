# Midjourney: generation

Generate remote content, start AI work, or consume quota.

| Command | Effect / risk | Exact usage | Arguments | Runtime |
|---|---|---|---|---|
| `generate` | `quota_consumption` / `high` | `opencli midjourney generate "<prompt>" [--model "<model>"] [--resolution "<resolution>"] [--speed "<speed>"] [--image-ref "<image-ref>"] [--style-ref "<style-ref>"] [--omni-ref "<omni-ref>"] [--image-weight "<image-weight>"] [--style-weight "<style-weight>"] [--omni-weight "<omni-weight>"] [--profile "<profile>"] [--repeat <repeat>] [--wait <true\|false>] [--index "<index>"] [--output "<output>"] [--skip-download <true\|false>] [--timeout <timeout>] [--dry-run <true\|false>] [--max-minutes "<max-minutes>"] [--reserve-minutes "<reserve-minutes>"] [--force <true\|false>] -f json`<br>Generate Midjourney images with references, model routing, cost guards, recovery, and optional download | `prompt` (str, required, positional); `model` (str, optional, default='auto'); `resolution` (str, optional, default='auto'); `speed` (str, optional, default='auto'); `image-ref` (str, optional); `style-ref` (str, optional); `omni-ref` (str, optional); `image-weight` (str, optional); `style-weight` (str, optional); `omni-weight` (str, optional); `profile` (str, optional); `repeat` (int, optional); `wait` (boolean, optional, default=True); `index` (str, optional, default='all'); `output` (str, optional, default='~/Pictures/Midjourney'); `skip-download` (boolean, optional, default=False); `timeout` (int, optional, default=300); `dry-run` (boolean, optional, default=False); `max-minutes` (str, optional, default=2); `reserve-minutes` (str, optional, default=0); `force` (boolean, optional, default=False) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
