# Slock: write-actions

Change remote service state.

| Command | Effect / risk | Exact usage | Arguments | Runtime |
|---|---|---|---|---|
| `channel-join` | `reversible_remote_write` / `high` | `opencli slock channel-join "<channel>" [--server "<server>"] -f json`<br>Join a public channel (POST /channels/:id/join) | `channel` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `channel-leave` | `reversible_remote_write` / `high` | `opencli slock channel-leave "<channel>" [--server "<server>"] -f json`<br>Leave a channel (POST /channels/:id/leave) | `channel` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `channel-mark` | `reversible_remote_write` / `high` | `opencli slock channel-mark "<channel>" [--seq <seq>] [--unread <true\|false>] [--server "<server>"] -f json`<br>Mark a channel read (default), read up to --seq, or --unread. | `channel` (str, required, positional); `seq` (int, optional); `unread` (bool, optional, default=False); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `channel-unarchive` | `reversible_remote_write` / `high` | `opencli slock channel-unarchive "<channel>" [--server "<server>"] -f json`<br>Unarchive a channel — admin only (POST /channels/:id/unarchive). #name lookups exclude archived channels; pass the channelId UUID for archived ones. | `channel` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `inbox-done` | `reversible_remote_write` / `high` | `opencli slock inbox-done "<channel>" [--server "<server>"] -f json`<br>Mark one chat as done / clear it from the inbox (POST /channels/inbox/done) | `channel` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `inbox-read-all` | `reversible_remote_write` / `high` | `opencli slock inbox-read-all [--server "<server>"] -f json`<br>Mark the entire inbox as read (POST /channels/inbox/read-all) | `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `reaction-add` | `reversible_remote_write` / `high` | `opencli slock reaction-add "<messageId>" "<emoji>" [--server "<server>"] -f json`<br>Add an emoji reaction to a message (POST /messages/:id/reactions). Idempotent server-side. | `messageId` (str, required, positional); `emoji` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `server-use` | `reversible_remote_write` / `high` | `opencli slock server-use "<input>" -f json`<br>Set the active slock server (writes localStorage.slock_last_server_slug) | `input` (str, required, positional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `task-claim` | `reversible_remote_write` / `high` | `opencli slock task-claim "<taskId>" [--server "<server>"] -f json`<br>Claim a chat task (PATCH /tasks/:id/claim). Requires full task UUID (= message id). | `taskId` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `task-convert` | `reversible_remote_write` / `high` | `opencli slock task-convert "<messageId>" [--server "<server>"] -f json`<br>Convert a message into a chat task (POST /tasks/convert-message). Accepts a message UUID or "#channel:shortId". | `messageId` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `task-status` | `reversible_remote_write` / `high` | `opencli slock task-status "<taskId>" "<status>" [--server "<server>"] -f json`<br>Set a task's status (PATCH /tasks/:taskId/status, body {status}). One of todo\|in_progress\|in_review\|done\|closed. | `taskId` (str, required, positional); `status` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `task-unclaim` | `reversible_remote_write` / `high` | `opencli slock task-unclaim "<taskId>" [--server "<server>"] -f json`<br>Release ownership of a chat task (PATCH /tasks/:id/unclaim). | `taskId` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `thread-done` | `reversible_remote_write` / `high` | `opencli slock thread-done "<threadChannelId>" [--server "<server>"] -f json`<br>Mark a thread as done / hide it from the active list (POST /channels/threads/done) | `threadChannelId` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `thread-undone` | `reversible_remote_write` / `high` | `opencli slock thread-undone "<threadChannelId>" [--server "<server>"] -f json`<br>Restore a done thread to the active list (POST /channels/threads/undone) | `threadChannelId` (str, required, positional); `server` (str, optional) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
