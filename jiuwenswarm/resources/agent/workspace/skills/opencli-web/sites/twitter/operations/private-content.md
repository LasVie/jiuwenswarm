# Twitter: private-content

Read content that depends on an authenticated account.

| Command | Effect / risk | Exact usage | Arguments | Runtime |
|---|---|---|---|---|
| `bookmark-folder` | `private_content_read` / `medium` | `opencli twitter bookmark-folder "<folder-id>" [--limit <limit>] [--top-by-engagement <top-by-engagement>] -f json`<br>Read the tweets inside a single Twitter/X bookmark folder. Get the folder id from `opencli twitter bookmark-folders`. | `folder-id` (string, required, positional); `limit` (int, optional, default=20); `top-by-engagement` (int, optional, default=0) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `bookmark-folders` | `private_content_read` / `medium` | `opencli twitter bookmark-folders -f json`<br>List your Twitter/X bookmark folders (the user-created collections under Bookmarks). Returns folder id, name, item count, and created_at. | none | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `bookmarks` | `private_content_read` / `medium` | `opencli twitter bookmarks [--limit <limit>] [--all <true\|false>] [--resume-file "<resume-file>"] [--output-file "<output-file>"] [--max-pages <max-pages>] [--top-by-engagement <top-by-engagement>] -f json`<br>Fetch your Twitter/X bookmarks (the logged-in user's saved tweets, newest first) | `limit` (int, optional, default=20); `all` (bool, optional, default=False); `resume-file` (string, optional); `output-file` (string, optional); `max-pages` (int, optional); `top-by-engagement` (int, optional, default=0) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `collection` | `private_content_read` / `medium` | `opencli twitter collection "<username>" --until "<until>" [--limit <limit>] [--page-delay <page-delay>] -f json`<br>Fetch a user timeline with relationship facts and a bounded completion receipt. | `username` (string, required, positional); `until` (string, required); `limit` (int, optional, default=10000); `page-delay` (int, optional, default=2) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `device-follow` | `private_content_read` / `medium` | `opencli twitter device-follow [--limit <limit>] [--top-by-engagement <top-by-engagement>] -f json`<br>Read the /i/timeline device-follow notification stream (tweets aggregated under a bell-icon "new posts from @userA and N others" notification) | `limit` (int, optional, default=20); `top-by-engagement` (int, optional, default=0) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `likes` | `private_content_read` / `medium` | `opencli twitter likes ["<username>"] [--limit <limit>] [--all <true\|false>] [--resume-file "<resume-file>"] [--output-file "<output-file>"] [--max-pages <max-pages>] [--top-by-engagement <top-by-engagement>] -f json`<br>Fetch liked tweets of a Twitter user (defaults to the logged-in user when no username is given) | `username` (string, optional, positional); `limit` (int, optional, default=20); `all` (bool, optional, default=False); `resume-file` (string, optional); `output-file` (string, optional); `max-pages` (int, optional); `top-by-engagement` (int, optional, default=0) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `list-tweets` | `private_content_read` / `medium` | `opencli twitter list-tweets "<listId>" [--limit <limit>] [--top-by-engagement <top-by-engagement>] -f json`<br>Fetch tweets from a Twitter/X list timeline | `listId` (string, required, positional); `limit` (int, optional, default=50); `top-by-engagement` (int, optional, default=0) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `lists` | `private_content_read` / `medium` | `opencli twitter lists [--limit <limit>] -f json`<br>Get Twitter/X lists for the logged-in user (owned + subscribed) | `limit` (int, optional, default=50) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |
| `notifications` | `private_content_read` / `medium` | `opencli twitter notifications [--limit <limit>] -f json`<br>Get your Twitter/X notifications (the logged-in user's likes/replies/follows feed, newest first) | `limit` (int, optional, default=20) | auth=required; transport=browser_intercept; fallback_before=browser_agent; fallback_after=none |
| `timeline` | `private_content_read` / `medium` | `opencli twitter timeline [--type "<for-you\|following>"] [--limit <limit>] [--top-by-engagement <top-by-engagement>] -f json`<br>Fetch the logged-in user's home timeline (for-you algorithmic feed by default; pass --type following for the chronological feed of accounts you follow) | `type` (str, optional, default='for-you', choices=for-you,following); `limit` (int, optional, default=20); `top-by-engagement` (int, optional, default=0) | auth=required; transport=browser_cookie; fallback_before=browser_agent; fallback_after=none |

## Operation-specific constraints

- `bookmark-folder`: Source-audited against OpenCLI 1.8.6 twitter/bookmark-folder.js; reads authenticated or session-scoped content that can expose private account data.; sensitive output: private social content, account identifiers
- `bookmark-folders`: Source-audited against OpenCLI 1.8.6 twitter/bookmark-folders.js; reads authenticated or session-scoped content that can expose private account data.; sensitive output: private social content, account identifiers
- `bookmarks`: Source-audited against OpenCLI 1.8.6 twitter/bookmarks.js; reads authenticated or session-scoped content that can expose private account data.; sensitive output: private social content, account identifiers
- `collection`: sensitive output: private content, account identifiers
- `device-follow`: Source-audited against OpenCLI 1.8.6 twitter/device-follow.js; reads authenticated or session-scoped content that can expose private account data.; sensitive output: private social content, account identifiers
- `likes`: Source-audited against OpenCLI 1.8.6 twitter/likes.js; reads authenticated or session-scoped content that can expose private account data.; sensitive output: private social content, account identifiers
- `list-tweets`: Source-audited against OpenCLI 1.8.6 twitter/list-tweets.js; reads authenticated or session-scoped content that can expose private account data.; sensitive output: private social content, account identifiers
- `lists`: Source-audited against OpenCLI 1.8.6 twitter/lists.js; reads authenticated or session-scoped content that can expose private account data.; sensitive output: private social content, account identifiers
- `notifications`: Source-audited against OpenCLI 1.8.6 twitter/notifications.js; reads authenticated or session-scoped content that can expose private account data.; sensitive output: private social content, account identifiers
- `timeline`: Source-audited against OpenCLI 1.8.6 twitter/timeline.js; reads authenticated or session-scoped content that can expose private account data.; sensitive output: private social content, account identifiers
