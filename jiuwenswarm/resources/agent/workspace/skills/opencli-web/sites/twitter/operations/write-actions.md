# Twitter: write-actions

Change remote service state.

| Command | Effect / risk | Exact usage | Arguments | Runtime |
|---|---|---|---|---|
| `accept` | `reversible_remote_write` / `high` | `opencli twitter accept "<query>" [--max <max>] [--timeout <timeout>] -f json`<br>Auto-accept DM requests containing specific keywords | `query` (string, required, positional); `max` (int, optional, default=20); `timeout` (int, optional, default=600) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
| `list-add` | `reversible_remote_write` / `high` | `opencli twitter list-add "<listId>" "<username>" -f json`<br>Add a user to a Twitter/X list you own (no-op if already a member) | `listId` (string, required, positional); `username` (string, required, positional) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
| `list-add-batch` | `reversible_remote_write` / `high` | `opencli twitter list-add-batch "<listId>" "<usernames>" [--interval <interval>] [--timeout <timeout>] -f json`<br>Add multiple users to a Twitter/X list you own from a comma-separated username list | `listId` (string, required, positional); `usernames` (string, required, positional); `interval` (int, optional, default=5); `timeout` (int, optional, default=600) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
| `quote` | `reversible_remote_write` / `high` | `opencli twitter quote "<url>" "<text>" [--image "<image>"] [--image-url "<image-url>"] -f json`<br>Quote-tweet a specific tweet with your own text, optionally with a local or remote image | `url` (string, required, positional); `text` (string, required, positional); `image` (str, optional); `image-url` (str, optional) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
| `retweet` | `reversible_remote_write` / `high` | `opencli twitter retweet "<url>" -f json`<br>Retweet a specific tweet | `url` (string, required, positional) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
| `unblock` | `reversible_remote_write` / `high` | `opencli twitter unblock "<username>" -f json`<br>Unblock a Twitter user | `username` (string, required, positional) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
| `unbookmark` | `reversible_remote_write` / `high` | `opencli twitter unbookmark "<url>" -f json`<br>Remove a tweet from bookmarks | `url` (string, required, positional) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
| `unlike` | `reversible_remote_write` / `high` | `opencli twitter unlike "<url>" -f json`<br>Remove a like from a specific tweet | `url` (string, required, positional) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
| `unretweet` | `reversible_remote_write` / `high` | `opencli twitter unretweet "<url>" -f json`<br>Undo a retweet on a specific tweet | `url` (string, required, positional) | auth=required; transport=browser_dom; fallback_before=browser_agent; fallback_after=none |
