schema_version: 2
catalog_version: opencli-1.8.6
site: 51job
display_name: 51Job
domains:
- jobs.51job.com
- we.51job.com
aliases: []
terminal: operation
operations:
  content:
    purpose: Read one public item, record, page, or resource.
    commands:
    - company
    - detail
  discovery:
    purpose: Search, browse, recommend, or discover site content.
    commands:
    - hot
    - search
commands:
  company:
    operation: content
    semantic_effect: public_read
    risk: low
    auth: none
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: 'Source-audited against OpenCLI 1.8.6 51job/company.js; reads public browser-rendered content without authenticated account state.'
  detail:
    operation: content
    semantic_effect: public_read
    risk: low
    auth: none
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: 'Source-audited against OpenCLI 1.8.6 51job/detail.js; reads public browser-rendered content without authenticated account state.'
  hot:
    operation: discovery
    semantic_effect: public_read
    risk: low
    auth: none
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: 'Source-audited against OpenCLI 1.8.6 51job/hot.js; reads public browser-rendered content without authenticated account state.'
  search:
    operation: discovery
    semantic_effect: public_read
    risk: low
    auth: none
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: 'Source-audited against OpenCLI 1.8.6 51job/search.js; reads public browser-rendered content without authenticated account state.'
