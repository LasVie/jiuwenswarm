schema_version: 1
catalog_version: opencli-1.8.6
site: brave
display_name: Brave
domains:
- search.brave.com
aliases: []
terminal: operation
review:
  status: generated
  owner: agent-c
  notes: Initial conservative policy; non-public-read commands remain fail-closed until their review wave.
operations:
  web-search:
    purpose: Read browser-rendered web search results.
    commands:
    - search
commands:
  search:
    operation: web-search
    semantic_effect: public_read
    risk: low
    auth: none
    transport: browser_dom
    execution_state: enabled
    executor: browser_manifest_public_read
    confirmation: none
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: Source-audited against OpenCLI 1.8.6 brave/search.js; reads public browser-rendered content without authenticated account state.
