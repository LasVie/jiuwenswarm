schema_version: 2
catalog_version: opencli-1.8.6
site: gov-law
display_name: Gov Law
domains:
- flk.npc.gov.cn
aliases: []
terminal: operation
operations:
  content:
    purpose: Read site content and metadata.
    commands:
    - recent
  discovery:
    purpose: Search, browse, recommend, or discover site content.
    commands:
    - search
commands:
  recent:
    operation: content
    semantic_effect: public_read
    risk: low
    auth: none
    transport: browser_dom
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: Source-audited against OpenCLI 1.8.6 gov-law/recent.js; reads public browser-rendered content without authenticated account state.
  search:
    operation: discovery
    semantic_effect: public_read
    risk: low
    auth: none
    transport: browser_dom
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: Source-audited against OpenCLI 1.8.6 gov-law/search.js; reads public browser-rendered content without authenticated account state.
