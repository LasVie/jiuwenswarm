schema_version: 2
catalog_version: opencli-1.8.6
site: indeed
display_name: Indeed
domains:
- www.indeed.com
aliases: []
terminal: operation
operations:
  content:
    purpose: Read one public item, record, page, or resource.
    commands:
    - job
  discovery:
    purpose: Search, browse, recommend, or discover site content.
    commands:
    - search
commands:
  job:
    operation: content
    semantic_effect: public_read
    risk: low
    auth: none
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: 'Source-audited against OpenCLI 1.8.6 indeed/job.js; reads public browser-rendered content without authenticated account state.'
  search:
    operation: discovery
    semantic_effect: public_read
    risk: low
    auth: none
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: 'Source-audited against OpenCLI 1.8.6 indeed/search.js; reads public browser-rendered content without authenticated account state.'
