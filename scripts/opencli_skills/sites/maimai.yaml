schema_version: 2
catalog_version: opencli-1.8.6
site: maimai
display_name: Maimai
domains:
- maimai.cn
aliases: []
terminal: operation
operations:
  account:
    purpose: Read account identity or account-scoped metadata.
    commands:
    - whoami
  authentication:
    purpose: Open, change, or clear an authenticated browser session.
    commands:
    - login
  private-content:
    purpose: Read content that depends on an authenticated account.
    commands:
    - search-talents
commands:
  login:
    operation: authentication
    semantic_effect: auth_session_change
    risk: high
    auth: interactive
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: none
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: ''
  search-talents:
    operation: private-content
    semantic_effect: private_content_read
    risk: high
    auth: required
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: none
    file_inputs: []
    file_outputs: []
    sensitive_output:
    - personal data
    - account identifiers
    notes: Source-audited against OpenCLI 1.8.6 maimai/search-talents.js; reads authenticated enterprise talent-search results containing candidate personal data.
  whoami:
    operation: account
    semantic_effect: private_account_read
    risk: medium
    auth: required
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: none
    file_inputs: []
    file_outputs: []
    sensitive_output:
    - account identifiers
    notes: Source-audited against OpenCLI 1.8.6 maimai/auth.js; reads current browser-session or account-scoped metadata.
