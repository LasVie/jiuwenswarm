schema_version: 1
catalog_version: opencli-1.8.6
site: uiverse
display_name: Uiverse
domains:
- uiverse.io
aliases: []
terminal: operation
review:
  status: generated
  owner: agent-a
  notes: Initial conservative policy; non-public-read commands remain fail-closed until their review wave.
operations:
  arbitrary-execution:
    purpose: Adapter entry points that can execute arbitrary input.
    commands:
    - code
  private-content:
    purpose: Read content that depends on an authenticated account.
    commands:
    - preview
commands:
  code:
    operation: arbitrary-execution
    semantic_effect: arbitrary_execution
    risk: critical
    auth: required
    transport: browser_dom
    execution_state: quarantined
    executor: none
    confirmation: unsupported
    fallback_before: none
    fallback_after: none
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: ''
  preview:
    operation: private-content
    semantic_effect: private_content_read
    risk: medium
    auth: required
    transport: browser_dom
    execution_state: disabled
    executor: none
    confirmation: unsupported
    fallback_before: browser_agent
    fallback_after: none
    file_inputs: []
    file_outputs: []
    sensitive_output:
    - private content
    - account identifiers
    notes: ''
