schema_version: 2
catalog_version: opencli-1.8.6
site: upwork
display_name: Upwork
domains:
- upwork.com
- www.upwork.com
aliases: []
terminal: operation
operations:
  account:
    purpose: Read account identity or account-scoped metadata.
    commands:
    - whoami
  authentication:
    purpose: Open, change, or clear an authenticated browser session.
    commands:
    - login
  private-content:
    purpose: Read content that depends on an authenticated account.
    commands:
    - feed
  content:
    purpose: Read one public item, record, page, or resource.
    commands:
    - detail
  discovery:
    purpose: Search, browse, recommend, or discover site content.
    commands:
    - search
commands:
  detail:
    operation: content
    semantic_effect: public_read
    risk: low
    auth: required
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: 'Source-audited against OpenCLI 1.8.6 upwork/detail.js; the adapter requires a logged-in browser session, but returns public site content rather than account-private state.'
  feed:
    operation: private-content
    semantic_effect: private_content_read
    risk: medium
    auth: required
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: none
    file_inputs: []
    file_outputs: []
    sensitive_output:
    - private job recommendations
    - account identifiers
    notes: 'Source-audited against OpenCLI 1.8.6 upwork/feed.js; reads authenticated or session-scoped content that can expose private account data.'
  login:
    operation: authentication
    semantic_effect: auth_session_change
    risk: high
    auth: interactive
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: none
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: ''
  search:
    operation: discovery
    semantic_effect: public_read
    risk: low
    auth: required
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: 'Source-audited against OpenCLI 1.8.6 upwork/search.js; the adapter requires a logged-in browser session, but returns public site content rather than account-private state.'
  whoami:
    operation: account
    semantic_effect: private_account_read
    risk: medium
    auth: required
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: none
    file_inputs: []
    file_outputs: []
    sensitive_output:
    - account identifiers
    notes: 'Source-audited against OpenCLI 1.8.6 upwork/auth.js; reads current browser-session or account-scoped metadata.'
