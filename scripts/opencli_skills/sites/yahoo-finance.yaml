schema_version: 2
catalog_version: opencli-1.8.6
site: yahoo-finance
display_name: Yahoo Finance
domains:
- finance.yahoo.com
aliases: []
terminal: operation
operations:
  analytics:
    purpose: Read aggregate metrics, trends, or rankings.
    commands:
    - quote
commands:
  quote:
    operation: analytics
    semantic_effect: public_read
    risk: low
    auth: optional
    transport: browser_cookie
    fallback_before: browser_agent
    fallback_after: browser_agent
    file_inputs: []
    file_outputs: []
    sensitive_output: []
    notes: Source-audited against OpenCLI 1.8.6 yahoo-finance/quote.js; reads public site content through the browser session. Login may affect availability, but no account-private fields are intended.
